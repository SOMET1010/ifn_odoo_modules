<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!--
        Vue de configuration IFN pour res.config.settings
        TEMPORAIREMENT DÉSACTIVÉE pour éviter les erreurs de champ manquant
        (merchant_portal_enabled, mobile_money_providers, etc.).

        Le module ifn_portal_merchant reste fonctionnel, mais sans bloc
        spécifique dans l'écran Paramètres.
        -->
    </data>
</odoo>
